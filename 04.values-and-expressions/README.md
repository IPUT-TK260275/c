# 04. 値と式

この章では、計算、比較、文字列操作、型変換を学びます。

「式」は、計算して 1 つの値になるものです。たとえば `1 + 2` は `3` になり、`a < b` は `true` または `false` になります。

## この章のゴール

| 節 | 学ぶこと |
| --- | --- |
| 4.1 | 数値、四則演算、数学関数 |
| 4.2 | 比較、真偽値、論理演算 |
| 4.3 | 文字列、長さ、切り出し、検索、型変換 |

## 数値と計算

よく使う演算子です。

| 書き方 | 意味 |
| --- | --- |
| `a + b` | 足し算 |
| `a - b` | 引き算 |
| `a * b` | 掛け算 |
| `a / b` | 割り算 |
| `a % b` | 余り |

例です。

```js
let price = 120;
let count = 5;
Lib.print(price * count);
Lib.print("\n");
```

## 比較

比較の結果は `true` または `false` です。

| 書き方 | 意味 |
| --- | --- |
| `a === b` | 等しい |
| `a !== b` | 等しくない |
| `a < b` | `a` が `b` より小さい |
| `a <= b` | `a` が `b` 以下 |
| `a > b` | `a` が `b` より大きい |
| `a >= b` | `a` が `b` 以上 |

比較で `=` を使わないでください。`=` は代入です。比較には `===` を使います。

## 論理演算

複数の条件を組み合わせます。

| 書き方 | 意味 |
| --- | --- |
| `a && b` | `a` かつ `b` |
| `a || b` | `a` または `b` |
| `!a` | `a` ではない |

たとえば、`x` が 1 以上 10 以下か調べるならこうです。

```js
let ok = 1 <= x && x <= 10;
```

`1 <= x <= 10` とは書きません。

## 文字列

文字列は `"` で囲みます。

```js
let s = "hello";
```

よく使う操作です。

| 書き方 | 意味 |
| --- | --- |
| `s.length` | 文字列の長さ |
| `s.charAt(0)` | 0 番目の文字 |
| `s.indexOf("e")` | `"e"` が最初に出る位置 |
| `s.slice(1, 4)` | 1 番目から 4 番目の手前まで取り出す |
| `s + t` | 文字列をつなげる |

JavaScript の位置番号は 0 から始まります。最初の文字は 1 番目ではなく 0 番目です。

## 型変換

入力は最初、文字列として読まれます。

```js
let s = Lib.input();
```

数値として計算したいときは、`Number(...)` を使います。

```js
let n = Number(Lib.input());
```

出力用に文字列へ変えたいときは、`String(...)` を使えます。

```js
let s = String(123);
```

## 問題ファイルの目安

| ファイル | 内容 |
| --- | --- |
| `4.1.*.js`, `Numbers.*.js` | 数値と計算 |
| `4.2.*.js`, `Boolean.*.js`, `Comparison-Operators.*.js` | 比較と真偽値 |
| `4.3.*.js`, `String.*.js` | 文字列操作 |
| `Type-Conversion.*.js` | 型変換 |

## よくあるミス

| ミス | 直し方 |
| --- | --- |
| 比較で `=` を使う | `===` を使う |
| 範囲判定を `a < b < c` と書く | `a < b && b < c` と書く |
| 入力した数が計算できない | `Number(Lib.input())` にする |
| 文字列の最初を 1 番目だと思う | 最初は 0 番目 |
| `slice` の終わり位置を含めてしまう | 終わり位置の手前まで取る |

## 確認コマンド

```bash
node --check 04.values-and-expressions/4.1.E1.js
node 04.values-and-expressions/Numbers.Arithmetic-Operators.1.js
```
