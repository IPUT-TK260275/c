# 07. 繰り返し

この章では、`while` を使って同じ処理を何度も実行する方法を学びます。

繰り返しは、プログラミングでとても重要です。「1 から 10 まで表示する」「入力された数を全部足す」「条件を満たすまで続ける」といった処理で使います。

## この章のゴール

| 学ぶこと | 例 |
| --- | --- |
| `while` の形 | `while (i < 10) { ... }` |
| カウンタ | `i = i + 1` |
| 合計を作る | `sum = sum + x` |
| 条件が変わるまで続ける | `while (条件)` |
| 実行の流れを追う | `.trace.txt` |

## `while` の基本形

```js
let i = 0;
while (i < 5) {
    Lib.print(i);
    Lib.print("\n");
    i = i + 1;
}
```

このコードは `0` から `4` までを表示します。

大事なのは、ループの中で `i = i + 1;` のように条件が変わる処理を書くことです。これがないと、繰り返しが終わりません。

## カウンタ

何回目かを数える変数をカウンタと呼びます。

```js
let i = 1;
while (i <= 10) {
    Lib.print(i);
    Lib.print("\n");
    i = i + 1;
}
```

## 合計

合計を作るときは、最初に `0` を入れておきます。

```js
let sum = 0;
let i = 1;
while (i <= 10) {
    sum = sum + i;
    i = i + 1;
}
Lib.print(sum);
Lib.print("\n");
```

`sum = sum + i;` は「今までの合計に `i` を足して、もう一度 `sum` に入れる」という意味です。

## 問題ファイルの目安

| ファイル | 内容 |
| --- | --- |
| `While.1.js`, `While.2.js` | `while` の基本 |
| `Counter.1.js` から `Counter.3.js` | カウンタ |
| `Accumulator.1.js`, `Accumulator.2.js` | 合計 |
| `Iteration.1.js`, `Iteration.2.js` | 繰り返しの例 |
| `Calculation-on-Iteration.*.js` | 繰り返しながら計算 |
| `7.4.E*.js` | 練習問題 |
| `7.4.A*.js` | 応用問題 |
| `7.4.R*.js` | 復習・発展問題 |
| `*.trace.txt` | 実行の流れを書く問題 |

## 解くときの手順

1. 何を繰り返すか決める。
2. 何回繰り返すか、またはいつ終わるか決める。
3. カウンタや合計用の変数を最初に作る。
4. `while` の条件を書く。
5. ループの中でカウンタを更新する。
6. 最後に答えを出力する。

## よくあるミス

| ミス | 直し方 |
| --- | --- |
| 無限ループになる | カウンタを更新しているか確認する |
| 1 回多い、または少ない | `<` と `<=`、初期値、終了条件を見る |
| 合計が変になる | `sum` を最初に `0` にしているか確認する |
| 出力が横に続く | 必要な場所で `"\n"` を出す |
| ループの外に書くべき処理を中に書く | 毎回やる処理か、最後に 1 回だけやる処理か分ける |

## 確認コマンド

```bash
node --check 07.iteration/7.4.E1.js
node 07.iteration/7.4.E1.js
./grademe.sh 07.iteration/7.4.E1.js
```

入力が必要な問題では、入力例に合わせて `printf` を使います。

```bash
printf '5\n' | node 07.iteration/7.4.E1.js
```

## 問題別の詳しい解説

各問題をどう考えればよいかは、同じフォルダの [EXPLANATIONS.md](./EXPLANATIONS.md) に分けてまとめています。READMEで章の全体像をつかんだあと、問題を解く前にそちらを読んでください。
